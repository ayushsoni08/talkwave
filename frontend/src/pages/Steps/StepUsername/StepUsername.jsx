import React from 'react'

const StepUsername = ({handleClick}) => {
  return (
    <>
      <div>StepUsername</div>
      <button onClick={handleClick}>Next</button>
    </>
  )
}

export default StepUsername